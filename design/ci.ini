┌─────────────────┐
│   Developer     │
│  (Git Commit)   │
└────────┬────────┘
         │
         ▼
┌────────────────────────────────┐
│            GitHub              │
│  Monorepo (3 Microservices)    │
│  - Node.js (Patient)           │
│  - Node.js (Appointment)       │
│  - Java Service                │
└────────┬───────────────────────┘
         │  Push / PR
         ▼
┌────────────────────────────────────────────────┐
│           GitHub Actions (CI Pipeline)         │
│------------------------------------------------│
│  Checkout Source Code                         │
│  Build Docker Images                          │
│    - Node & Java builds inside Docker         │
│  Tag Images (latest / commit-sha)             │
│  Authenticate to GCP                          │
│  Push Images to Artifact Registry             │
└────────┬───────────────────────────────────────┘
         │   🔐 No Secrets Stored
         ▼
┌────────────────────────────────────────────────┐
│        Google Artifact Registry                │
│------------------------------------------------│
│ patient-service:latest                          │
│ appointment-service:latest                      │
│ java-service:latest                             │
└────────────────────────────────────────────────┘
